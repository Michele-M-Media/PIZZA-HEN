
## R7.25.1 - SVT Play + Media Player Services correction

- Corrects the R7.25 optional-player integration to match the requested behavior.
- ProsperoPlayer 1.0, PS-Play 2.1 and BFplayer standalone alpha.44 are deployed byte-for-byte to `/data/PIZZA_HEN/payloads/` and shown as normal Services switches.
- Removed PIZZA HEN INSTALL/DELETE staging, `/builtin/media` copies and the custom Prospero/PS-Play conflict blocker.
- No player is launched by the bootstrapper; users choose START/STOP from Services.
- SVT Play remains a top-level direct Web App entry and does not launch the BREW10002 installer ELF.
## R7.23.2 — Remote Play Direct Injection
- Rebased from the user-supplied R7.23.1 archive.
- Replaced the embedded/frozen Remote Play payload with the newly supplied v0.1.1 ELF byte-for-byte (same verified SHA-256).
- Remote Play now injects `/data/PIZZA_HEN/payloads/rp-get-pin.elf` directly through the existing `/hbldr` path on every click.
- Removed Remote Play button dependence on managed process state, plugin-launch, plugin-stop and autostart mutation.
- No modification to the Remote Play ELF.

## R7.23.1 — Remote Play PIN Notification Repair
- Fixed PIZZA success notification masking the upstream Remote Play PIN/Account ID notification.
- Successful Remote Play launch is log-only in the manager; upstream payload notifications remain visible.
- Failure/stop notifications, managed process verification, NO-PKG/NO-TILE behavior preserved.

# R7.23 — PIZZA HEN Payload Repository

- Replaced the legacy remote Payload/Plugin repository index with a PIZZA HEN-owned built-in catalog generated from the user-supplied `json/payloads.json`.
- Frozen source contains 83 entries; 79 `.elf` entries are accepted and 4 `.bin` entries are intentionally excluded by the current ELF-only Plugin Manager policy.
- Removed runtime dependency on `itsplk.github.io/ps5-payloads-mirror/payloads.json`.
- `AGGIORNA REPOSITORY` now restores/re-publishes the built-in PIZZA HEN catalog.
- Individual ELF downloads preserve the supplied URLs and require safe filename, ELF magic and matching SHA-256 before installation.
- Repository status/install feedback reuses existing translated keys across all 31 locales.
- Added reproducible generator and R7.23 static gate.

# R7.22.1 — Remote Play + Tools Naming Repair

- Remote Play moved from direct `/hbldr` fire-and-forget launch to managed Payload/Plugin launch with real runtime verification.
- Remote Play second press uses graceful SIGTERM stop (upstream handler invalidates pairing PIN), with bounded SIGKILL fallback.
- APR title normalized to `Apr Emu Update`.
- Toolbox `Settings` renamed to localized `Tools` (`Strumenti` in Italian) across all 31 locales.
- No PKG/tile behavior changed; no new boot autostart.

# R7.20.2 — Full Toolbox NO-PKG / NO-TILE integration

- Homebrew Channel: PIZZA-integrated websrv 0.34 keeps `/index.html` but disables the upstream `install_launcher()` startup call.
- APR EMU UPDATE 1.4: disables automatic launcher thread, manual tile-install endpoint, and tile-specific WebUI controls while keeping TCP 6971 updater functions.
- Game Compressor 1.0.4: disables `gc_launcher_start()` while keeping the worker and TCP 5910 WebUI.
- Frozen upstream binaries remain untouched; runtime variants are generated by a SHA/preimage-gated derivation tool.
- All PIZZA HEN copy for the three integrations remains covered by the 31-language policy.
- No boot-time ShellUI preload/injection added.

## R7.19 — Six user-supplied Services + full 31-language integration
- Added AirPSX 0.19 and PS5Upload 5.4.8 as resident Services with real TCP-state switches (1214 and 9114).
- Added PS5 FW Spoof, NP Fake Signin 1.3, WebKit Autoloader Installer and PS5 App Dumper 1.11 as managed on-demand service/task switches.
- One-shot payload switches represent process state only; no fake undo behavior is claimed.
- Added PIZZA HEN localized START/STOP/FAIL notifications and 31-language descriptions/status text.
- All six user-supplied ELF files are frozen byte-for-byte and embedded/deployed under `/data/PIZZA_HEN/payloads/`.
- No new R7.19 payload auto-starts at boot; anti-CE-108262 boot preload rollback remains intact.


## R7.18 — KStuff Base + ShadowMount skip for dump_installer
- Added user-supplied KStuff 1.6.7 as Base selector choice (source profiles 3.00 through 10.01).
- Added `Do not launch ShadowMountPlus` choice for EchoStretch dump_installer users.
- Skip choice continues Media tiles → FTP → ps5debug-NG → remaining pipeline.
- Stable/Experimental ShadowMount bytes unchanged.
# R7.6.1 - Plugin Scan / Service Runtime Repair

- Restores util startup behavior to the pre-Payload-Manager R7.3 path; Payload Repository remains on-demand only.
- Plugin scan publishes the same catalog to /data and /user/data runtime aliases.
- Scanner covers PIZZA HEN roots plus legacy etaHEN roots already present in supplied etaHEN source/logs for migration compatibility.
- Web Toolbox reads either catalog alias.
- Direct ELF launch path avoids the PS5 WebKit `Cannot access uninitialized variable` failure observed on hardware.
- No changes to KStuff selector, Debug Services route, Package Installer, Cheats route, or supplied service ELF bytes.

# Changelog

## v0.1-beta

Initial public beta checkpoint.

### Core
- PIZZA HEN identity, data root and Toolbox.
- Graphical KStuff Lite 1.09 / KStuff DR 1.2 boot selector.
- Single-KStuff-per-boot ownership.
- Frozen pristine ShadowMountPlus 1.6beta16 integration.
- Automatic ftpsrv and ps5debug-NG chain.
- Media tile launcher and safe Toolbox reopen behavior.

### Toolbox
- PIZZA HEN UI/branding cleanup.
- Unified cheats sources.
- Direct Itemzflow Game Manager integration.
- Overlay GPU/CPU/RAM sections disabled by default.
- Redundant legacy PS5Debug setting removed from UI.

### etaHEN 2.6B observed delta
- App Plugin Manager beta.
- `[DEFAULT]` and per-title plugin sections.
- `?autoload` persistence.
- Session lifecycle/status tracking.
- PS4/PS5 FPS section naming/update.

### Build
- Capability-based Multi-SDK discovery.
- Current, legacy and override toolchain paths.
- Standard-library-only host regression tests.

### Known beta limitation
- App plugins already loaded into a running app are not forcibly hot-unloaded when autoload is disabled.

## R7.6.3 - Hidden legacy Toolbox hosts restore
- Restored the exact R7.2 DOM panels for System Options, Rest Mode Options, Controller Shortcuts, Extras / Firmware Backends, Homebrew Store and PS5 webMAN Games.
- These panels remain hidden from the visible Toolbox menus, matching the existing hidden Game Manager pattern.
- Preserved R7.6.2.1 CE-108262 rollback: Toolbox open does not auto-run `game-options-ensure` / ShellUI injection.
- Preserved R7.6.1 plugin scan, KlogSrv, Fan Target, PHU/Pizza Overlay, KStuff Toggle and Payload Manager code paths.


## PIZZA HEN v1.0 — CheatRunner v0.17 Toolbox integration
- Retired legacy game-options/native PIZZA cheat route.
- Vendored and source-builds CheatRunner v0.17.
- Toolbox starts CheatRunner on demand and embeds its dashboard/API on port 9999.
- Old PIZZA cheat cache startup and Toolbox cheat IPC actions are disabled.

## PIZZA HEN v1.0 — DPIv2 12.20+ etaHEN 2.6B MetaInfo repair
- Baseline remains the tester Compile Repair (`9f9dafef...b9308783`).
- Debug Services / Onion multi-firmware routing is unchanged.
- DPIv2 port 12800 and AppInstUtil backend are unchanged.
- For URL installs on firmware 12.20+ only, `MetaInfo` now follows the observed etaHEN 2.6B call shape: zero-initialize all optional pointers and set only `uri`.
- Firmware <= 12.00 retains the prior etaHEN/PIZZA DPIv2 URL behavior unchanged at runtime.
- Upload-to-console branch is unchanged.
- This is source/static validated only until hardware-tested on 12.20+.


## R7.15.2 — ELF Loader 0.24 TCP runtime-state repair
- Hardware video confirmed R7.15.1 displayed the START notification but immediately reset the Toolbox switch with `runtime state did not match request`.
- ELF Loader state is now sourced from the actual local TCP listener on `127.0.0.1:9021`, not the generic plugin PID catalogue.
- START and STOP remain the real existing actions; no cosmetic forced-ON state was added.
- Toolbox polls the real TCP state for up to 6 seconds after a transition before accepting/rejecting the switch state.
- APR EMU UPDATE, ShadowMount selector/auto-close, FTP, ps5debug-NG, KStuff, CheatRunner and DPIv2 paths are unchanged.

## R7.16 — Homebrew Channel
- Adds a top-level **Homebrew Channel** item to the PIZZA HEN Toolbox.
- Opens the existing websrv 0.34 `/index.html` homebrew launcher directly in the current WebView.
- No Launcher PKG is installed or required by this integration.
- No new runtime payload is introduced; websrv remains the owner of homebrew discovery/launch.
- R7.15.2 ELF Loader TCP state repair and all prior selector/service paths are preserved.

## R7.22 — Web File Manager + Linux Loader + Game Download
- Added top-level **Web File Manager** using user-supplied PS5 Web File Manager 1.5. PIZZA's derived ELF disables the upstream `FMGR88888` launcher installer while keeping the local WebUI/file operations.
- Added top-level **Linux Loader** using user-supplied ps5-linux-loader 2.4. It is firmware-gated to the upstream 3.00–7.61 list and requests KStuff `BOTH_PAUSED` before the one-shot launch. No launcher PKG/tile is created by PIZZA HEN.
- Added **Game Download** submenu with **Pegasus DL 1.7.0** (TCP 6970) and **Spectrum Library 1.4.2** (TCP 7575).
- Pegasus automatic/manual launcher installation is disabled in the PIZZA derived ELF.
- Spectrum `SLIB00001` installer worker is disabled in the PIZZA derived ELF.
- Added managed-process + TCP state validation for Web File Manager, Pegasus DL and Spectrum Library. Web File Manager discovers its actual listen port in the 8888–8919 range.
- Added all PIZZA-controlled R7.22 strings to all 31 supported locales in the same changeset.


## R7.25 — SVT Play + Optional Media Players
- Added top-level **SVT Play** using the exact `webAppUri` embedded in the user-supplied v0.2 ELF; the BREW10002 installer ELF is not launched, so PIZZA HEN creates no SVT PKG/tile.
- Added ProsperoPlayer 1.0, PS-Play 2.1 and BFplayer standalone alpha.44 to **Services** as optional installable ELFs backed by `/data/PIZZA_HEN/builtin/media/`.
- Added INSTALL / START-STOP / DELETE controls with real Plugin Manager PID state and no automatic autostart.
- Added runtime conflict protection for ProsperoPlayer and PS-Play because both supplied ELFs use `PRSP10001` and TCP 9055.
- BFplayer DELETE removes only the PIZZA HEN-managed ELF copy and does not claim to uninstall an existing PSMC00001 Media tile.
- Added all new PIZZA HEN text in all 31 locales.
## R7.25.1 — Media player Services simplification
- Removed the Optional Media Players group label and custom INSTALL/DELETE UI.
- ProsperoPlayer, PS-Play and BFplayer are byte-exact user-supplied ELF files exposed as ordinary Services ON/OFF switches.
- Removed custom player conflict/interlock logic and builtin/media staging.
- SVT Play behavior unchanged.



## R7.25.2 — Remote Play Service Switch
- Replaced Remote Play special direct injection UI with the same managed ON/OFF service switch used by the working media-player service rows.
- User-supplied `ps5-remoteplay-get-pin` v0.1.1 preserved byte-for-byte.
- No autostart, PKG or tile added.
